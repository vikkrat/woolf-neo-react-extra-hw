import LoginForm from '../../components/LoginForm/LoginForm';

const LoginPage = () => {
  return (
    <section>
      <h1>Login</h1>
      <LoginForm />
    </section>
  );
};

export default LoginPage;
